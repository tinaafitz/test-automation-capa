# Feature: STS Mode
# Applies to: create (immutable after creation)
apiVersion: capa-automation/v1
kind: ClusterAutomationSpec
metadata:
  name: sts
spec:
  action: create
  cluster: ''
  namespace: ns-rosa-hcp
  features:
    sts: true
